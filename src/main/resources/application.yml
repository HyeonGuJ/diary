server:
  port: 8089

spring:
  jpa:
    database: MYSQL
    properties.hibernate:
      format_sql: true
      show_sql: true
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/_diary
    username: root
    password:
  redis:
    host: localhost
    port: 6379